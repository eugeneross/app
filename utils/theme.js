
import colors from './colors'
import transitions from './transitions'

const radius = {
    rounded: '.25rem',
    superRounded: '.5rem',
    pill: '10rem'
  }

export const theme = {
  colors,
  transitions,
  radius
}
